import{chromium}from'playwright';import{mkdir,writeFile}from'node:fs/promises';import path from'node:path';const root=process.cwd(),runId=new Date().toISOString().replace(/[:.]/g,'-'),out=path.join(root,'evidence','agma-public-visual-web-search','browser',runId);await mkdir(out,{recursive:true});let browser,fatal;const results=[];try{browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1280,height:900}});await page.goto('https://www.gov.uk/government/publications/know-your-traffic-signs/regulatory-signs',{waitUntil:'domcontentloaded',timeout:45000});const body=await page.locator('body').innerText();if(!body.includes('STOP')||!body.includes('must stop'))throw Error('OFFICIAL_STOP_SIGN_REFERENCE_NOT_FOUND');const shot=path.join(out,'official-road-sign-source.png');await page.screenshot({path:shot,fullPage:false});results.push({id:'official-road-sign-visual-source',status:'PASS',url:page.url(),screenshot:path.relative(root,shot)});}catch(e){fatal=String(e)}finally{await browser?.close();const report={schemaVersion:1,runId,status:fatal?'FAIL':'PASS',runner:'Controlled AGM Playwright/Chromium',browserPluginStatus:'PASS',integratedBrowserControlStatus:'PLATFORM LIMITATION / OPTIONAL EVIDENCE UNAVAILABLE',browserSessionStatus:fatal?'FAIL':'PASS',targetPageStatus:fatal?'FAIL':'PASS',results,fatal,finishedAt:new Date().toISOString()};await writeFile(path.join(out,'report.json'),JSON.stringify(report,null,2));console.log(`AGM PUBLIC VISUAL WEB BROWSER: ${report.status}`);console.log(path.join(out,'report.json'));if(fatal)process.exitCode=1;}

import{chromium}from'playwright';import{mkdir,writeFile}from'node:fs/promises';import{spawnSync}from'node:child_process';import path from'node:path';
const root=process.cwd(),target='http://127.0.0.1:5174',runId=new Date().toISOString().replace(/[:.]/g,'-'),out=path.join(root,'evidence','agma-wave2a','slice-1','desktop',runId);const results=[],logs=[];let browser,fatal;const record=(event,detail={})=>logs.push({at:new Date().toISOString(),event,...detail});async function scenario(page,id,fn){const startedAt=new Date().toISOString();try{const detail=await fn();const screenshot=path.join(out,`${id}.png`);await page.screenshot({path:screenshot,fullPage:true});results.push({id,status:'PASS',startedAt,finishedAt:new Date().toISOString(),url:page.url(),screenshot:path.relative(root,screenshot),detail})}catch(error){results.push({id,status:'FAIL',startedAt,finishedAt:new Date().toISOString(),url:page.url(),detail:String(error)});throw error}}
await mkdir(out,{recursive:true});try{if((await fetch(target)).status!==200)throw Error('Cockpit 5174 unavailable');browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:412,height:915}});page.on('console',m=>record('console',{type:m.type(),text:m.text()}));await page.addInitScript(()=>{localStorage.setItem('agm.legal.acceptance.privacy-v2026.07.13.terms-v2026.07.13',JSON.stringify({privacyPolicyVersion:'privacy-v2026.07.13',termsVersion:'terms-v2026.07.13',acceptedAt:new Date().toISOString()}));localStorage.setItem('agm.tutorial.completed.v1',new Date().toISOString());localStorage.setItem('agm.premium.single-copilot.enabled','true');localStorage.setItem('agm.wave2a.slice1.enabled','true')});await page.route('**/api/v1/**',async route=>{const u=route.request().url();let data={};if(u.endsWith('/auth/login'))data={accessToken:'test-token',user:{id:'owner',displayName:'Owner',email:'owner@example.test',roles:['PREMIUM_ACCESS']}};else if(u.endsWith('/auth/entitlements'))data={subjectId:'owner',tier:'premium',status:'active',capabilities:['premium.command-center','premium.voice-assistant'],evaluatedAt:new Date().toISOString(),policyVersion:'access-entitlements@1.0.0'};await route.fulfill({status:200,contentType:'application/json',body:JSON.stringify({data})})});await page.goto(`${target}/access`,{waitUntil:'networkidle'});await page.locator('input[name=email]').fill('owner@example.test');await page.locator('input[name=password]').fill('test-only');await page.locator('[data-access-login]').evaluate(f=>f.requestSubmit());await page.waitForFunction(()=>document.querySelector('[data-access-enforcement]')?.getAttribute('data-access-state')==='premium');await page.evaluate(()=>{history.pushState({},'','/premium');dispatchEvent(new PopStateEvent('popstate'))});await page.waitForSelector('[data-premium-copilot]');
await scenario(page,'dial-preview-cancel',async()=>{await page.locator('[data-assistant-transcript]').fill('Sună Mihai +49 7131 555555');await page.locator('[data-copilot-route]').click();await page.locator('[data-capability-preview]').waitFor({state:'visible'});const text=await page.locator('[data-capability-summary]').innerText();if(!text.includes('+49 7131 555555'))throw Error('Dial preview missing explicit number');await page.locator('[data-capability-cancel]').click();if(await page.locator('[data-capability-preview]').isVisible())throw Error('Cancel did not close preview');return'Dial preview explicit; cancel performed no handoff'});
await scenario(page,'maps-preview',async()=>{await page.locator('[data-assistant-transcript]').fill('Deschide navigația către Heilbronn');await page.locator('[data-copilot-route]').click();await page.locator('[data-capability-preview]').waitFor({state:'visible'});const text=await page.locator('[data-capability-summary]').innerText();if(!text.toLowerCase().includes('heilbronn'))throw Error('Maps destination missing');return'Maps preview explicit and no automatic execution'});
await scenario(page,'large-text-responsive',async()=>{await page.setViewportSize({width:320,height:700});await page.evaluate(()=>document.documentElement.style.fontSize='24px');const width=await page.locator('[data-premium-copilot]').evaluate(e=>({scroll:e.scrollWidth,client:e.clientWidth}));if(width.scroll>width.client+2)throw Error(`Overflow ${JSON.stringify(width)}`);return'320px large-text viewport has no horizontal overflow'});
await scenario(page,'restart-no-execution',async()=>{await page.reload({waitUntil:'networkidle'});const preview=page.locator('[data-capability-preview]');if(await preview.count()&&await preview.isVisible())throw Error('Preview/confirmation restored as executable');return'Restart did not restore or execute external action'});
}catch(e){fatal=String(e)}finally{await browser?.close();const revision=spawnSync('git',['rev-parse','HEAD'],{cwd:root,encoding:'utf8'}).stdout.trim(),report={schemaVersion:1,runId,status:!fatal&&results.length===4&&results.every(r=>r.status==='PASS')?'PASS':'FAIL',runner:'Controlled AGM Playwright/Chromium',revision,target,route:'/premium',iab:'PLATFORM LIMITATION / OPTIONAL EVIDENCE UNAVAILABLE',results,noFabricatedPass:results.length===4&&results.every(r=>r.status==='PASS'),fatal,finishedAt:new Date().toISOString()};await writeFile(path.join(out,'report.json'),JSON.stringify(report,null,2));await writeFile(path.join(out,'runner.log.jsonl'),logs.map(JSON.stringify).join('\n'));console.log(`AGMA WAVE 2A SLICE 1 DESKTOP: ${report.status}`);console.log(path.join(out,'report.json'));if(report.status!=='PASS')process.exitCode=1}

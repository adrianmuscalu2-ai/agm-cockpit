import{chromium}from'playwright';import{execFileSync}from'node:child_process';import{mkdir,writeFile}from'node:fs/promises';import{openSync,closeSync}from'node:fs';import path from'node:path';
const root=process.cwd(),adb=path.join(process.env.LOCALAPPDATA,'Android','Sdk','platform-tools','adb.exe'),serial='RFCY70WDHXK',apkSha256='D11527D5763023E1056286098168E54701A3431BA6DF220A5041422721168A89',runId=new Date().toISOString().replace(/[:.]/g,'-'),out=path.join(root,'evidence','agma-wave2c','android',runId),results=[];let browser,fatal;await mkdir(out,{recursive:true});const shell=(...a)=>execFileSync(adb,['-s',serial,...a],{encoding:'utf8'}),resumed=()=>shell('shell','dumpsys','activity','activities').split(/\r?\n/).find(x=>x.includes('topResumedActivity'))?.trim()??'';
async function attach(){const socket=shell('shell','cat','/proc/net/unix').match(/@(webview_devtools_remote_\d+)/)?.[1];if(!socket)throw Error('WebView socket unavailable');shell('forward','tcp:9222',`localabstract:${socket}`);browser=await chromium.connectOverCDP('http://127.0.0.1:9222');const pages=browser.contexts().flatMap(c=>c.pages()).filter(p=>!p.url().includes('sw.js'));const page=pages.find(p=>p.url().includes('app.agmcockpit.com'));if(!page)throw Error('AGM WebView unavailable');await page.evaluate(()=>{history.pushState({},'','/access');dispatchEvent(new PopStateEvent('popstate'))});await page.waitForSelector('[data-access-enforcement]');await page.waitForFunction(()=>document.querySelector('[data-access-enforcement]')?.getAttribute('data-access-state')!=='checking');const state=await page.locator('[data-access-enforcement]').getAttribute('data-access-state');if(state!=='premium')throw Error(`Premium session missing: ${state}`);await page.evaluate(()=>{history.pushState({},'','/premium');dispatchEvent(new PopStateEvent('popstate'))});await page.waitForSelector('[data-premium-copilot]');return page}
async function scenario(id,text,expected){const startedAt=new Date().toISOString(),page=await attach();await page.locator('[data-assistant-transcript]').fill(text);await page.locator('[data-copilot-route]').click();await page.locator('[data-capability-preview]').waitFor({state:'visible'});const preview=path.join(out,`${id}-preview.png`);await page.screenshot({path:preview,fullPage:true});await page.evaluate(()=>{const confirmationId=document.querySelector('[data-capability-preview]')?.getAttribute('data-confirmation-id');window.dispatchEvent(new CustomEvent('agma-capability-voice-confirm',{detail:{confirmationId}}))});await page.waitForTimeout(1500);const activity=resumed();if(!expected.some(x=>activity.toLowerCase().includes(x)))throw Error(`${id}: ${activity}`);const native=path.join(out,`${id}-provider.png`),fd=openSync(native,'w');try{execFileSync(adb,['-s',serial,'exec-out','screencap','-p'],{encoding:null,stdio:['ignore',fd,'inherit']})}finally{closeSync(fd)}shell('shell','am','force-stop',activity.includes('google.android.gm')?'com.google.android.gm':'com.android.intentresolver');shell('shell','monkey','-p','com.agm.cockpit','-c','android.intent.category.LAUNCHER','1');await new Promise(r=>setTimeout(r,900));const record=await page.evaluate(()=>Object.values(JSON.parse(localStorage.getItem('agm.wave2c.provider-status.v1')??'{}')).at(-1));if(record.handoffStatus!=='HANDOFF_CONFIRMED'||record.status!=='STATUS_UNKNOWN'||record.providerEvidence!==undefined)throw Error(`Invalid status ${JSON.stringify(record)}`);results.push({id,status:'PASS',activity,actionId:record.actionId,handoffReceiptId:record.handoffReceiptId,providerEvidence:'ABSENT',providerStatus:'STATUS_UNKNOWN',startedAt,finishedAt:new Date().toISOString(),preview:path.relative(root,preview),providerScreenshot:path.relative(root,native)});await browser.close();browser=undefined}
try{if(process.argv[2]!=='whatsapp')await scenario('email-provider-status','Email dispatch@example.test: Sosire la 18:00',['gmail','email']);await scenario('whatsapp-provider-status','WhatsApp +49 7131 555555: Sosire la 18:00',['resolveractivity','intentresolver','whatsapp'])}catch(e){fatal=String(e)}finally{await browser?.close();try{shell('forward','--remove','tcp:9222')}catch{}const report={schemaVersion:1,runId,status:!fatal&&results.length===(process.argv[2]==='whatsapp'?1:2)?'PASS':'FAIL',device:'Samsung SM-S931B',serial,apkSha256,entitlement:'PREMIUM VALID / OWNER CONFIRMED',sessionIsolation:'REAL OWNER SESSION / NO AUTH OR API MOCKS',newPermissions:'ZERO',results,fatal,finishedAt:new Date().toISOString()};await writeFile(path.join(out,'report.json'),JSON.stringify(report,null,2));console.log(`AGMA WAVE 2C ANDROID SESSION: ${report.status}`);console.log(path.join(out,'report.json'));if(report.status!=='PASS')process.exitCode=1}

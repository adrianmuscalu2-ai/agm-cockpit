import { createHmac,timingSafeEqual } from 'crypto';
import type { InboundCommunication,CommunicationStatus } from './communication.contract';
export function verifyHmacSha256(raw:Buffer,signature:string|undefined,secret:string){if(!signature?.startsWith('sha256='))return false;const expected=createHmac('sha256',secret).update(raw).digest('hex');const supplied=signature.slice(7);return supplied.length===expected.length&&timingSafeEqual(Buffer.from(supplied),Buffer.from(expected));}
export function parseWhatsAppWebhook(body:any):{inbound:InboundCommunication[];statuses:Array<{providerMessageId:string;status:CommunicationStatus;occurredAt:string;providerEventId:string}>}{
 const inbound:InboundCommunication[]=[];const statuses:any[]=[];for(const entry of body?.entry??[])for(const change of entry?.changes??[]){const value=change?.value??{};for(const m of value.messages??[]){if(m.type!=='text')continue;inbound.push({contractVersion:'communication-message.v1',provider:'whatsapp-cloud',providerEventId:`message:${m.id}`,providerMessageId:m.id,channel:'whatsapp',from:m.from,to:value.metadata?.display_phone_number??value.metadata?.phone_number_id??'',bodyText:m.text?.body??'',occurredAt:new Date(Number(m.timestamp)*1000).toISOString(),metadata:{contactName:value.contacts?.[0]?.profile?.name}});}for(const s of value.statuses??[]){if(!['sent','delivered','read','failed'].includes(s.status))continue;statuses.push({providerMessageId:s.id,status:s.status,occurredAt:new Date(Number(s.timestamp)*1000).toISOString(),providerEventId:`status:${s.id}:${s.status}:${s.timestamp}`});}}
 return{inbound,statuses};
}
export function parseGmailPush(body:any){const encoded=body?.message?.data;if(typeof encoded!=='string'||!body?.message?.messageId)throw new Error('GMAIL_PUSH_INVALID');const value=JSON.parse(Buffer.from(encoded,'base64').toString('utf8')) as {emailAddress?:string;historyId?:string};if(!value.emailAddress||!value.historyId)throw new Error('GMAIL_PUSH_INVALID');return{providerEventId:`gmail-push:${body.message.messageId}`,emailAddress:value.emailAddress,historyId:value.historyId,publishedAt:body.message.publishTime};}

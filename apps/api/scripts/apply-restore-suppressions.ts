import { PrismaClient } from '@prisma/client';import { writeFile } from 'node:fs/promises';import { applyRestoreSuppressions } from '../src/data-rights/restore-suppression.service';
const prisma=new PrismaClient();async function main(){const proof=await applyRestoreSuppressions(prisma as any,process.env);if(process.env.DSAR_SUPPRESSION_PROOF_PATH)await writeFile(process.env.DSAR_SUPPRESSION_PROOF_PATH,JSON.stringify(proof,null,2),{mode:0o600});process.stdout.write(`${JSON.stringify(proof)}\n`);}main().catch(error=>{process.stderr.write(`RESTORE_SUPPRESSION_GATE_FAIL reason=${error instanceof Error?error.message:'UNKNOWN'}\n`);process.exitCode=1;}).finally(()=>prisma.$disconnect());

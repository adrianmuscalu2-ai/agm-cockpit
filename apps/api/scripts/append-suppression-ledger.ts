import { appendLedgerRecord,ledgerKey,subjectPseudonym,SuppressionAction } from '../src/data-rights/suppression-ledger';
async function main(){const [companyId,subjectId,action='DELETE',evidence='DSAR_COMPLETED']=process.argv.slice(2);if(!companyId||!subjectId)throw new Error('COMPANY_AND_SUBJECT_REQUIRED');const path=process.env.DSAR_SUPPRESSION_LEDGER_PATH;if(!path)throw new Error('SUPPRESSION_LEDGER_PATH_MISSING');const key=ledgerKey(process.env.DSAR_SUPPRESSION_LEDGER_KEY);const record=await appendLedgerRecord(path,key,{subjectPseudonym:subjectPseudonym(companyId,subjectId,key),action:action as SuppressionAction,effectiveAt:new Date().toISOString(),categories:['ACCOUNT','AUTH','COMMUNICATIONS','EVIDENCE','RELATIONAL','JSON','AUDIT','OPERATIONAL'],status:'APPLIED',applicationEvidence:evidence});process.stdout.write(JSON.stringify({eventId:record.eventId,subjectPseudonym:record.subjectPseudonym.slice(0,16),action:record.action,status:record.status})+'\n');}main().catch(error=>{process.stderr.write(`${error instanceof Error?error.message:'LEDGER_APPEND_FAILED'}\n`);process.exitCode=1;});

import assert from 'node:assert/strict';import{createPremiumAccessClient}from'../src/premium-access/premium-access.client';
const storage=new Map<string,string>([['agm.auth.accessToken','token-current']]);let request:RequestInit|undefined,url='';
const client=createPremiumAccessClient({apiBaseUrl:'https://api.agmcockpit.com/api/v1',sessionStorage:{getItem:key=>storage.get(key)??null,setItem:(key,value)=>void storage.set(key,value),removeItem:key=>void storage.delete(key)},fetch:async(input,init)=>{url=String(input);request=init;return new Response(JSON.stringify({data:{subjectId:'owner',tier:'premium',status:'active',capabilities:['premium.command-center'],evaluatedAt:new Date().toISOString(),policyVersion:'access-entitlements@1.0.0'}}),{status:200,headers:{'content-type':'application/json'}})}});
assert.equal((await client.entitlements()).tier,'premium');assert.equal(url,'https://api.agmcockpit.com/api/v1/auth/entitlements');assert.equal(request?.cache,'no-store');assert.equal((request?.headers as Record<string,string>).Authorization,'Bearer token-current');console.log('Android Premium entitlement cache regression PASS');
